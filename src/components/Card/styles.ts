import styled from 'styled-components';

export const Price = styled.p`
  background: var(--blue);
  padding: 1rem 0;
  text-align: center;
  font-size: 1.25rem;
  color: var(--text-color-secondary);
`;
